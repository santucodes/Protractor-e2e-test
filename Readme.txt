Step to execute:
1. git Clone 
Execute below commands are from inside the cloned folder
2. npm install -g protractor
3. npm install webdriver-manager or webdriver-manager install (Not sure)
4. webdriver-manager update
5. npm install -g typescript
6. npm install 
7. npm run tsc
8. protractor protractor.conf.js (This will run the test)
